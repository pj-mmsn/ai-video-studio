# AI Video Studio - 多模型视频工作室

# LLM API
openai>=1.0.0
python-dotenv>=1.0.0

# CLI
rich>=13.0.0

# 图像处理（Mock 模式用）
Pillow>=10.0.0

# 数据库（项目持久化 + 断点续传）
# sqlite3 是 Python 内置模块，无需额外安装

# Web 可视化界面
fastapi>=0.110.0
uvicorn>=0.29.0

# 语音合成（可选，为视频加旁白）
# edge-tts>=6.1.0
